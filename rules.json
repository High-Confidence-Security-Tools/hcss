{
    "rules": [
        {
            "id": "RSA private key",
            "regex": "-----BEGIN RSA PRIVATE KEY-----"
        },
        {
            "id": "GitHub access token",
            "regex": "[g|G][i|I][t|T][h|H][u|U][b|B].*['|\"][0-9a-zA-Z]{35,40}['|\"]"
        }
    ]
}
